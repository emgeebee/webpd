import { TrackNamePipe } from './track-name.pipe';

describe('TrackNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TrackNamePipe();
    expect(pipe).toBeTruthy();
  });
});
